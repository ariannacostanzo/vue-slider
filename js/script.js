// console.log(Vue)

const {createApp} = Vue;

const app = createApp ({
    name: 'image slider',
    data() {
        return {
            destinations,
            currentImageIndex: 0, 
        }
    },





});


app.mount('#root')